name: template-full-stack-reagent

on:
  push:
    branches: [main]
    paths:
      - 'templates/full-stack-reagent/**'
      - 'cli/**'
      - '.github/workflows/template-full-stack-reagent.yml'
      - 'verification/**'
  pull_request:
    paths:
      - 'templates/full-stack-reagent/**'
      - 'cli/**'
      - '.github/workflows/template-full-stack-reagent.yml'
      - 'verification/**'
  workflow_dispatch:

jobs:
  hook-iso-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: DeLaGuardo/setup-clojure@13.6.1
        with:
          bb: 1.12.218
      - name: Run c3kit-template.bb isolation test
        run: |
          cd templates/full-stack-reagent
          bb spec/hook_test.bb

  coupling-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: DeLaGuardo/setup-clojure@13.6.1
        with:
          bb: 1.12.218
      - name: Static feature-coupling scan
        run: |
          cd verification
          bb scan-coupling

  verify-light:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        combo:
          - memory-minimal
          - memory-content-no-ssr
          - memory-no-websocket
          - sqlite-no-auth
          - postgres-ssr-no-content
          - datomic-pro-no-ssr-no-content
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
      - uses: DeLaGuardo/setup-clojure@13.6.1
        with:
          cli: latest
          bb: 1.12.218
          clj-kondo: latest
          cljfmt: latest
      - name: Install Playwright browser
        run: npx --yes playwright install --with-deps chromium
      - name: Verify (light)
        run: |
          cd verification
          bb verify --combo ${{ matrix.combo }} --tier light \
            --cli-cp ${{ github.workspace }}/cli/src

  verify-full:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
      - uses: DeLaGuardo/setup-clojure@13.6.1
        with:
          cli: latest
          bb: 1.12.218
          clj-kondo: latest
          cljfmt: latest
      - name: Install Playwright browser
        run: npx --yes playwright install --with-deps chromium
      - name: Verify (full, memory-defaults)
        run: |
          cd verification
          bb verify --combo memory-defaults --tier full \
            --cli-cp ${{ github.workspace }}/cli/src
