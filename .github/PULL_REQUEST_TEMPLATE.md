## Linked issue

**Required.** This PR closes:

Closes #

> PRs without a linked, maintainer-acknowledged issue will be closed without review. See [CONTRIBUTING.md](../CONTRIBUTING.md).

## Summary

What does this PR change and why?

## Test plan

- [ ] `bb test` passes (in `cli/`)
- [ ] `bb lint` passes (in `cli/`)
- [ ] `shellcheck cli/install.sh` passes (if installer touched)
- [ ] Template CI passes (if `templates/**` touched)
- [ ] `CHANGES.md` updated (if user-visible)
- [ ] Docs / specs updated (if behavior or roadmap changed)
